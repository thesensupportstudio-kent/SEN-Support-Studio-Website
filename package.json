{
  "name": "sen-support-studio-website",
  "private": true,
  "scripts": {
    "build:functions": "esbuild build-src/pdf.js --bundle --platform=browser --format=esm --outfile=functions/api/_lib/pdf.js"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
